{
  "name": "@cca/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript contracts between the CCA platform API and web app",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
